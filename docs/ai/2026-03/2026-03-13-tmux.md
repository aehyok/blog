如果还没安装 WSL

先在管理员 PowerShell 里运行：

wsl --install

重启后，系统会让你完成 Ubuntu 初始化，设置：

Linux 用户名

Linux 密码

完成后就能用了。

wsl -l -v

![[Pasted image 20260313163634.png]]


 wsl --list --verbose

如果不是默认设置为默认
wsl --set-default Ubuntu


sudo apt update
sudo apt install -y tmux











