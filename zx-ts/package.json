{
  "name": "zx",
  "version": "2.0.0.0",
  "description": "",
  "main": "index.mts",
  "type": "module",
  "scripts": {
    "build:pc": "ts-node-esm index.mts -v 1.0.0.007 -p pc",
    "build:all": "concurrently \"pnpm build:app\"  \"pnpm build:pc\"  \"pnpm build:wechat\"  \"pnpm build:park\"  \"pnpm build:qrcode\" ",
    "prebuild": "ts-node-esm pull.mts -v 1.0.0.007",
    "build": "concurrently \"pnpm build:pc\"",
    "postbuild": "ts-node-esm push.mts -v 1.0.0.007",
    "release-major": "standard-version --release-as major -x x.0.0",
    "release-minor": "standard-version --release-as minor -x 0.x.0",
    "release-patch": "standard-version --release-as patch -x 0.0.x",
    "release": "standard-version"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.8.0",
    "typescript": "^4.7.2",
    "zx": "^5.2.0"
  },
  "dependencies": {
    "@types/node": "*",
    "concurrently": "^7.2.1",
    "fs-extra": "^10.1.0",
    "mysql": "^2.18.1",
    "shortid": "^2.2.16",
    "standard-version": "^9.3.2"
  }
}
